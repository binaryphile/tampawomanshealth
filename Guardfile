# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload', grace_period: 1 do
  watch(%r{pages})
  watch(%r{posts})
  watch(%r{partials})
  watch(%r{themes})
  watch(%r{data.yml})
end

